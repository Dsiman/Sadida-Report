// Edit this when deploying to production.
//
// Local dev: run `vercel dev` in /api and point at http://localhost:3000
// Production: point at the deployed Vercel function URL.

export const API_BASE = 'https://sadida-report.vercel.app';
export const CURRENT_MOD_VERSION_DEFAULT = 'v0.3.22';
