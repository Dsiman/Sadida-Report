# Copy to .env.local for local dev (Vercel CLI reads it).
# Never commit .env or .env.local.

MONGODB_URI=mongodb+srv://report-writer:REPLACE_ME@sadida.xxxxx.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB=reports
ALLOWED_ORIGIN=http://localhost:8080
IP_SALT=replace-with-openssl-rand-hex-16
